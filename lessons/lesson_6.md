# JSON в JavaScript
JSON - формат обмена данными, основанный на синтаксисе JavaScript. В JS объекты представляются с помощью коллекций пар ключ-значение.

JSON.stringify();  // Объект -> JSON-строка
JSON.parse();  // JSON -> Объект

# API 
Набор правил, протоколов, инструментов, позволяющих различным приложениям взаимодействовать друг с другом. 
Типы API:
– REST API
– GraphQL
– SOAP
– WebSocket

# Асинхронность 
Позволяет выполнять долгие операции (запросы к серверу или чтение файлов) без блокировки основного потока выполнения. 

Заказ пиццы - мы не ждем курьера у двери, а занимаемся другими делами, пока пиццу не доставят. 

JavaScript движок - синхронный
- может делать только одно дело за раз
– быстро выполняет простые задачи
– не умеет ждать 

web api (помощники) - дают асинхронность
setTimeout - таймер помощник
fetch - курьер для запросов
addEventListener - слушатель событий
filereader - читатель файлов

event loop (диспетчер) - связывает всю работу движка и асинхронности

